# Wallet private key used for deployment (testnet only — never commit real keys)
PRIVATE_KEY=0xyour_private_key_here

# Ritual Chain RPC (defaults to the public endpoint if omitted)
RITUAL_RPC_URL=https://rpc.ritualfoundation.org

# RITUAL to deposit into RitualWallet for the contract on deploy (default 0.5)
FUND_RIT=0.5
